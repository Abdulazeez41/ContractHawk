{
  "name": "bug_hunter_agent",
  "version": "1.0.0",
  "description": "A smart contract auditor agent using AgentOpera, capable of static analysis with Slither and explanation generation using LLM.",
  "main": "index.js",
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0"
  }
}
